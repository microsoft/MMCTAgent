# =============================================================================
# Lively Video Ingestion & Search Configuration
# =============================================================================
# Copy this file to .env and fill in your values
# This .env file is used by both ingestion and mcp_server modules

# =============================================================================
# Azure AI Search Configuration
# =============================================================================
# Your Azure AI Search service endpoint
SEARCH_SERVICE_ENDPOINT=https://your-search-service.search.windows.net

# Name of the search index (can use same index for multiple videos)
SEARCH_INDEX_NAME=video-keyframes-index

# Optional: Azure Search API key (fallback if not using Azure CLI)
# Recommended: Use Azure CLI authentication instead (run 'az login')
SEARCH_API_KEY=

# =============================================================================
# Azure Blob Storage Configuration
# =============================================================================
# Your Azure Storage account URL
AZURE_STORAGE_ACCOUNT_URL=https://yourstorageaccount.blob.core.windows.net

# Optional: Storage connection string (fallback if not using Azure CLI)
# Recommended: Use Azure CLI authentication instead
AZURE_STORAGE_CONNECTION_STRING=

# Optional: Container name for keyframes (default: keyframes)
KEYFRAME_CONTAINER_NAME=keyframes

# =============================================================================
# MCP Server Configuration
# =============================================================================
# Default number of results to return from search
DEFAULT_TOP_K=10

# =============================================================================
# Authentication Notes
# =============================================================================
# RECOMMENDED: Use Azure CLI authentication
#   1. Run: az login
#   2. Leave SEARCH_API_KEY and AZURE_STORAGE_CONNECTION_STRING empty
#
# ALTERNATIVE: Use API keys / connection strings
#   1. Provide SEARCH_API_KEY and/or AZURE_STORAGE_CONNECTION_STRING
#   2. Less secure but useful for CI/CD or non-interactive scenarios
