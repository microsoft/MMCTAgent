# ==========================================
# Authentication Configuration
# ==========================================
# Recommended: Use Azure CLI authentication
# Run 'az login' before using the pipeline
# No additional configuration needed!

# ==========================================
# Azure Storage Configuration
# ==========================================
# Option 1 (Recommended): Storage Account URL - Uses Azure CLI / Managed Identity
AZURE_STORAGE_ACCOUNT_URL=https://your_account_name.blob.core.windows.net

# Option 2 (Fallback): Connection String
# AZURE_STORAGE_CONNECTION_STRING=DefaultEndpointsProtocol=https;AccountName=your_account_name;AccountKey=your_account_key;EndpointSuffix=core.windows.net

# ==========================================
# Azure AI Search Configuration
# ==========================================
# Required: Search Service Endpoint
SEARCH_SERVICE_ENDPOINT=https://your-search-service.search.windows.net

# Optional (Fallback): Search API Key - Only needed if not using Azure CLI
# SEARCH_API_KEY=your_search_api_key

# ==========================================
# Optional: Container Names
# ==========================================
KEYFRAME_CONTAINER_NAME=keyframes

# ==========================================
# Optional: Device Configuration
# ==========================================
# Options: "cpu", "cuda", "auto"
DEVICE=auto
